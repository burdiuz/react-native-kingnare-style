// modal with trnsparent background, may be closed when user taps outside
const Modal = () => null;
// modal with blocking background( a little more transparent ), does not close when clicked outside
const Modal = () => null;
